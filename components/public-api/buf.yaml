version: v1
breaking:
  use:
    - WIRE_JSON
  ignore:
    # Do not enforce breaking change detection for the experimental package
    - gitpod/experimental
lint:
  use:
    - DEFAULT
  ignore:
    - google
