map $proxy_protocol_server_port $x_forwarded_proto {
    443      https;
    80       http;
    default  $scheme;
}

map $proxy_protocol_server_port $x_forwarded_host {
    ""      "$host:$server_port";
    default "$host:$proxy_protocol_server_port";
}
