apiVersion: kots.io/v1beta1
kind: ConfigValues
spec:
  values:
      domain:
        value: "${TF_VAR_TEST_ID}.gitpod-self-hosted.com"
        data: "domain"
      ssh_gateway:
        value: "1"
        data: "ssh_gateway"
