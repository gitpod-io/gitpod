apiVersion: kots.io/v1beta1
kind: ConfigValues
spec:
  values:
      reg_incluster:
        value: "0"
        data: "reg_incluster"
      reg_url:
        value: "gcr.io/sh-automated-tests"
        data: "reg_url"
      reg_server:
        value: "gcr.io"
        data: "reg_server"
      reg_username:
        value: "_json_key"
        data: "reg_username"
      reg_password:
        value: "${GCP_KEY}"
        data: "reg_password"
