packages:
  - name: docker
    type: docker
    deps:
      - install/installer:app
    argdeps:
      - imageRepoBase
    srcs:
      - "entrypoint.sh"
      - "manifests/*.yaml"
    config:
      dockerfile: leeway.Dockerfile
      image:
        - ${imageRepoBase}/preview-install:${version}
